{% extends 'base.html.twig' %}

{% block title %}
    {{ ad.title }}
{% endblock %}

{% block body %}

    <a href="{{ path("app_homepage") }}">Home</a>

    <h1>{{ ad.title }}</h1>
    <p>{{ ad.description }}</p>
    <p>Prix : {{ ad.price|round(2, 'floor') }} $</p>

    <a href="/user/{{ ad.user.id }}" class="btn btn-outline-warning">Voir le vendeur</a>

{% endblock %}